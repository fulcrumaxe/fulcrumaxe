[pytest]
asyncio_mode = auto
addopts = --import-mode=importlib
markers =
    integration: live integration tests (require network + real state dir; opt-in with -m integration)
    timeout: per-test time bound in seconds, enforced by pytest-timeout (declared in requirements.txt)
