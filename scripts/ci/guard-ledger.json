{
  "note": "Files in scripts/ci/ that scripts/ci/run-guards.sh deliberately does NOT run. 'exempt' means the file runs nowhere at all; 'own_step' means a workflow invokes it directly because the runner cannot give it what it needs. scripts/ci/guard-registry-check.py fails the build when a file is in neither section nor in the runner's discovered set, when an entry has an empty reason, when an entry names a file that no longer exists, when an 'exempt' file turns out to be wired to a workflow after all, when an 'own_step' file is referenced by no workflow, and when a file is both discovered by the runner and given its own workflow step. Every reason has to be checkable by a reviewer — 'not a guard' is not a reason.",
  "exempt": {
    "backend-module-cwd-exempt.json": "Data, not a runnable file (D#2325): the exemption ledger read by scripts/ci/backend-module-cwd-guard.py, which the runner discovers and runs. Checkable by opening that guard — it loads this file by name and fails the build on an entry with an empty reason or an entry naming a script that no longer has an unanchored call. Running it as a guard is not possible; it is JSON.",
    "clean-install-check.sh": "Local-only developer tool (D#1617, PR #1623): it builds a throwaway venv on a dev host to reproduce what CI already does. The backend (import-smoke) job IS that clean install — it pip-installs only requirements.txt onto a fresh runner and then runs the same scripts/ci/backend-import-smoke.py — so as a CI step this would nest a venv inside an already-clean runner and add the get-pip.py network fetch its ensurepip fallback makes, which nothing else in this workflow needs.",
    "merge-gate-parity-ledger.json": "Data, not a runnable file (D#2332): the merge-path divergence ledger read by scripts/ci/merge-gate-parity-guard.py, which the runner discovers and runs. Checkable by opening that guard — it loads this file by name and fails the build on an entry with an empty reason, on an entry claiming a label scripts/merge-and-hook.sh now enforces, on an entry claiming a label scripts/loop-phased-step5.sh no longer gates, and on an entry whose 'assert' evidence no longer matches the tree. Running it as a guard is not possible; it is JSON."
  },
  "own_step": {
    "backend-import-smoke.py": "The job's namesake and the precondition for everything the runner runs after it: every Python guard imports backend modules, so a broken import has to be reported as one import failure rather than as every guard failing at once. Runs as its own step in backend (import-smoke), before the runner.",
    "guard-registry-check.py": "Runs as its own step in backend (import-smoke) under `if: ${{ !cancelled() }}`, so it still reports when the runner step before it goes red. Inside the runner it could say nothing about a runner that failed to start — which is the one condition that would hide every guard at once.",
    "pr-link-policy.sh": "Runs in pr-gates.yml's `PR body link policy` job. It reads the PR body out of $GITHUB_EVENT_PATH into a PR_BODY_FILE the step writes first; there is no PR event payload in backend (import-smoke), and pr-gates.yml also needs the `edited` trigger so an edited body is re-checked.",
    "publish-denylist.sh": "Runs in pr-gates.yml's `publish denylist` job. It three-dot-diffs the PR head against PUBLISH_DENYLIST_BASE from the PR event and needs `fetch-depth: 0` for a real merge base in the object store; backend (import-smoke) checks out shallow and has neither.",
    "pr-mutation-evidence.sh": "Runs in pr-gates.yml's `PR mutation evidence` job (D#1984). Same reasons as pr-link-policy.sh above: it reads the PR body out of $GITHUB_EVENT_PATH into a PR_BODY_FILE the step writes first, and there is no PR event payload in backend (import-smoke) to give it. Also applies and reverts a real git patch against the checkout, which run-guards.sh's unconditional-run contract was never built to isolate from the rest of that job's guards.",
    "ts-backend-test-integrity.sh": "Runs as the ts-backend leg's own \"Test\" step in ci.yml's build-test matrix, wrapping the leg's existing `bun run test` call with a before/after hash of the tracked tree (D#2342). It needs the bun toolchain and the ts-backend workspace's installed deps, which only exist in that matrix leg after its own Setup Bun / Install dependencies (bun) steps; backend (import-smoke) runs on Python only and never sets up bun, so the runner cannot give it what it needs."
  }
}
