# tests/baselines/pytest/

A recorded measurement of this project's own Python suite on one host in September 2026, kept so the numbers in its Discussions stay checkable; it is not used by CI and nothing reads it at build time.
